/// Production WaterWise API origin (no trailing slash, no `/api` suffix).
/// Matches [deploy.sh] `VITE_API_BASE_URL` default.
const String kDefaultWaterwiseApiOrigin = 'https://api.waterwise.loficode.tech';
